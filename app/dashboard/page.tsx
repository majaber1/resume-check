"use client";
import { useEffect,useState } from "react";import Link from "next/link";import ResultCard from "@/components/ResultCard";import {createClient} from "@/lib/supabase/client";import type{AnalysisResult}from"@/lib/analysis";
type Item={id:string;title:string;overall_score:number;ats_score:number;result:AnalysisResult;created_at:string};
export default function Dashboard(){const[items,setItems]=useState<Item[]>([]);const[selected,setSelected]=useState<Item|null>(null);const[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/analyses").then(async r=>{if(r.status===401){location.href="/?signin=1";return}const d=await r.json();setItems(d.analyses||[]);setLoading(false)})},[]);async function signOut(){await createClient()?.auth.signOut();location.href="/"}return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5"><Link href="/" className="text-xl font-black text-indigo-600">✓ ResumeCheck</Link><button onClick={signOut} className="rounded-lg border px-4 py-2 text-sm font-bold">Sign out</button></div></header><div className="mx-auto max-w-6xl px-5 py-12"><div className="flex items-end justify-between"><div><p className="font-bold text-indigo-600">DASHBOARD</p><h1 className="mt-2 text-4xl font-black">Your analyses</h1></div><Link href="/#analyzer" className="rounded-xl bg-indigo-600 px-5 py-3 font-bold text-white">New analysis</Link></div>{loading?<p className="mt-12">Loading…</p>:items.length===0?<div className="mt-12 rounded-3xl border bg-white p-12 text-center"><h2 className="text-xl font-bold">No saved reports yet</h2></div>:<div className="mt-10 grid gap-6 lg:grid-cols-[320px_1fr]"><aside className="space-y-3">{items.map(x=><button key={x.id} onClick={()=>setSelected(x)} className="w-full rounded-2xl border bg-white p-4 text-left hover:border-indigo-400"><b className="block truncate">{x.title}</b><span className="mt-2 block text-xs text-slate-500">Overall {x.overall_score} · ATS {x.ats_score} · {new Date(x.created_at).toLocaleDateString()}</span></button>)}</aside><section>{selected?<ResultCard result={selected.result}/>:<div className="rounded-3xl border bg-white p-12 text-center text-slate-500">Select a report</div>}</section></div>}</div></main>}
